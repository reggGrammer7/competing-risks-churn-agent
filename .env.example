# Copy this file to .env (same folder) and fill in ONE of the keys below.
# .env is already in .gitignore -- it will never be committed or shared.
#
# You only need ONE of these. If both are set, Anthropic is used first.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
