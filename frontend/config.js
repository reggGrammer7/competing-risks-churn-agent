// The ONE line to edit when deploying: point this at your Render backend's
// URL (e.g. "https://churn-agent-backend.onrender.com") instead of the
// local default. Kept in its own file, loaded before app.js, so deploying
// to Vercel never means touching app.js itself -- just this one constant.
const API_BASE = "http://127.0.0.1:8000";
